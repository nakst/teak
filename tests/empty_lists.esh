str[] Test(int[] x) {
	assert x != null;
	assert x:len() == 0;
	return [];
}

void Start() {
	int[] x;
	x = [];
	int[] y = [];
	Test(y);
	Test([]);
	Log("%x%");
	Log("%y%");
	int[][] z = [[1,2,3],[4,5,6],[]];
}
